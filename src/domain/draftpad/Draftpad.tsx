import React from 'react';

import Card from '../../common/card/Card';
import Header from '../../common/header/Header';

const App: React.FC = () => (
  <Card width="50%" Header={() => <Header>Hakemukset(1)</Header>} />
);

export default App;
