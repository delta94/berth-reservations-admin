import React from 'react';

import UnmarkedWsNoticeList from './UnmarkedWsNoticeList';

const UnmarkedWsNoticeListContainer = () => {
  return <UnmarkedWsNoticeList />;
};

export default UnmarkedWsNoticeListContainer;
